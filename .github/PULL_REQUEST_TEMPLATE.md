## What changed

## Checklist

- [ ] `npm test`, `npm run lint`, and `npm run typecheck` pass locally
- [ ] Added a changeset if this touches `packages/core`, `packages/react`, or `packages/vue` (`npm run changeset`)
- [ ] Updated the relevant package's README if behavior or options changed
